<main>
    <div class="container-fluid px-4">
        <h1 class="mt-4">Valorização dos livros usados perante o preço base</h1>
        <ol class="breadcrumb mb-4">
            <li class="breadcrumb-item active">Vendas e compras</li>
        </ol>

        <div class="row container justify-content-center border m-auto mt-3 mb-3">

            <div class="row flex justify-content-between mt-2">

                <h6 class="mt-3">Venda a um cliente</h6>

                <div class="row flex justify-content-between mt-2">
                    <div class="col-xl-3 col-md-6">
                        <label class="form-label">Condição "excelente"</label>
                        <input class="form-control" value="<%= loadContent.percentages.percentagesToSale.excellent%> %" disabled>
                    </div>

                    <div class="col-xl-3 col-md-6">
                        <label class="form-label">Condição "bom"</label>
                        <input class="form-control" value="<%= loadContent.percentages.percentagesToSale.good%> %" disabled>
                    </div>

                    <div class="col-xl-3 col-md-6">
                        <label class="form-label">Condição "médio"</label>
                        <input class="form-control" value="<%= loadContent.percentages.percentagesToSale.medium%> %" disabled>
                    </div>

                    <div class="col-xl-3 col-md-6">
                        <label class="form-label">Condição "mau"</label>
                        <input class="form-control" value="<%= loadContent.percentages.percentagesToSale.bad%> %" disabled>
                    </div>
                </div>

                <h6 class="mt-5">Compra a um cliente</h6>

                <div class="row flex justify-content-between mt-2">
                    <div class="col-xl-3 col-md-6">
                        <label class="form-label">Condição "excelente"</label>
                        <input class="form-control" value="<%= loadContent.percentages.percentagesToPurchase.excellent%> %" disabled>
                    </div>

                    <div class="col-xl-3 col-md-6">
                        <label class="form-label">Condição "bom"</label>
                        <input class="form-control" value="<%= loadContent.percentages.percentagesToPurchase.good%> %" disabled>
                    </div>

                    <div class="col-xl-3 col-md-6">
                        <label class="form-label">Condição "médio"</label>
                        <input class="form-control" value="<%= loadContent.percentages.percentagesToPurchase.medium%> %" disabled>
                    </div>

                    <div class="col-xl-3 col-md-6 mb-5">
                        <label class="form-label">Condição "mau"</label>
                        <input class="form-control" value="<%= loadContent.percentages.percentagesToPurchase.bad%> %" disabled>
                    </div>
                </div>
            </div>
        </div>

    </div>
</main>